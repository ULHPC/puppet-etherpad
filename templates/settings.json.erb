/*
################################################################################
# settings.json
# Etherpad-lite configuration
#
################################################################################
#" /!\ DO NOT EDIT THIS FILE: it has been automatically generated by Puppet.
#      In particular, any further changes will be overwritten at the next puppet
#      invocation
################################################################################
*/
/*
  This file must be valid JSON. But comments are allowed

  Please edit settings.json, not settings.json.template
*/
{
  // Name your instance!
  "title": "<%= scope.lookupvar('etherpad::pad_title') %>",

  //Ip and port which etherpad should bind at
  "ip": "<%= scope.lookupvar('etherpad::ip') %>",
  "port" : <%= scope.lookupvar('etherpad::port') %>,

  // Session Key, used for reconnecting user sessions
  // Set this to a secure string at least 10 characters long. Do not share this value.
  "sessionKey" : "<%= scope.lookupvar('etherpad::session_key') %>",

  //The Type of the database. You can choose between dirty, postgres, sqlite and mysql
<% if scope.lookupvar('etherpad::dbtype') == 'dirty' %>
  //You shouldn't use "dirty" for for anything else than testing or development
  "dbType" : "dirty",
  //the database specific settings
  "dbSettings" : {
                   "filename" : "<%= scope.lookupvar('etherpad::filename') %>"
                 },
<% end %>
<% if scope.lookupvar('etherpad::dbtype') == 'mysql' %>
  /* MySQL Configuration
  */
   "dbType" : "mysql",
   "dbSettings" : {
                    "user"    : "<%= scope.lookupvar('etherpad::mysql_user') %>",
                    "host"    : "<%= scope.lookupvar('etherpad::mysql_host') %>",
                    "password": "<%= scope.lookupvar('etherpad::mysql_password') %>",
                    "database": "<%= scope.lookupvar('etherpad::mysql_database') %>"
                  },
<% end %>

  //the default text of a pad
  "defaultPadText" : "<%= scope.lookupvar('etherpad::defaultpadtext') %>",

  /* Users must have a session to access pads. This effectively allows only group pads to be accessed. */
  "requireSession" : false,

  /* Users may edit pads but not create new ones. Pad creation is only via the API. This applies both to group pads and regular pads. */
  "editOnly" : false,

  /* if true, all css & js will be minified before sending to the client. This will improve the loading performance massivly,
     but makes it impossible to debug the javascript/css */
  "minify" : <%= scope.lookupvar('etherpad::minify') %>,

  /* How long may clients use served javascript code (in seconds)? Without versioning this
     may cause problems during deployment. Set to 0 to disable caching */
  /* Default: 21600, 60 * 60 * 6 = 6 hours */
  "maxAge" : <%= scope.lookupvar('etherpad::maxage') %>,

<% if scope.lookupvar('etherpad::abiword') == 'present' %>
  /* This is the path to the Abiword executable. Setting it to null, disables abiword.
     Abiword is needed to enable the import/export of pads*/
  "abiword" : "<%= scope.lookupvar('etherpad::params::abiword_path') %>",
<% else %>
  "abiword" : null,
<% end %>

  /* This setting is used if you require authentication of all users.
     Note: /admin always requires authentication. */
  "requireAuthentication": false,

  /* Require authorization by a module, or a user with is_admin set, see below. */
  "requireAuthorization": false,

  /* Users for basic authentication. is_admin = true gives access to /admin.
     If you do not uncomment this, /admin will not be available! */

  "users": {
    "admin": {
      "password": "<%= scope.lookupvar('etherpad::admin_password') %>",
      "is_admin": true
    }
  },


  /* The log level we are using, can be: DEBUG, INFO, WARN, ERROR */
  "loglevel": "INFO",

  // restrict socket.io transport methods
  "socketTransportProtocols" : ['xhr-polling', 'jsonp-polling', 'htmlfile']
}
